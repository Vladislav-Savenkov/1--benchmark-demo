// Функции и процедуры для управления объектами эксплуатации

Процедура ДобавитьОбъект(Наименование, Адрес, ДатаНачалаЭксплуатации)
    НовыйОбъект = Catalogs.ОбъектыЭксплуатации.CreateItem();
    НовыйОбъект.Наименование = Наименование;
    НовыйОбъект.Адрес = Адрес;
    НовыйОбъект.ДатаНачалаЭксплуатации = ДатаНачалаЭксплуатации;
    НовыйОбъект.Write();
КонецПроцедуры

Функция НайтиОбъектПоНаименованию(Наименование) Экспорт
    Выборка = Catalogs.ОбъектыЭксплуатации.Select();
    Пока Выборка.Next() Цикл
        Если Выборка.Наименование = Наименование Тогда
            Возврат Выборка.Ref;
        КонецЕсли;
    КонецЦикла;
    Возврат Неопределено;
КонецФункции
